# Student Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** พีรพงศ์ ปัญญาสัน
- **Student ID:** 67543210042-7
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)

## 📂 Project Structure
- public/ ทำหน้าแยกส่วนของ Frontend ให้ชัดเจน (HTML, CSS, JS) และใช้สำหรับจัดเก็บไฟล์ที่ถูกส่งไปยัง browser โดยตรง
- src/ เป็นส่วนของโค้ดฝั่ง Backend ซึ่งถูกแบ่งตามแนวคิด Layered Architecture
- presentation รับผิดชอบการสื่อสารระหว่าง frontend และระบบ backend → Controller / Routes / Middleware
- business เป็นชั้นที่จัดการกฎและตรรกะทางธุรกิจของระบบ → Service + Validation
- data รับผิดชอบการจัดการข้อมูลและการเข้าถึงฐานข้อมูล → Repository + Database

## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
* แก้ไขและดูแลยาก
  * โค้ดทั้งหมดผูกกันแน่น การแก้ไขจุดหนึ่งอาจกระทบทั้งระบบ 
* การขยายระบบไม่ยืดหยุ่น
  * ไม่สามารถขยายเฉพาะส่วนที่ใช้งานหนัก ต้องขยายทั้งระบบพร้อมกัน
* พัฒนาพร้อมกันหลายคนลำบาก
  * ทีมงานหลายคนทำงานในโค้ดเดียวกัน เกิดความซ้ำซ้อนและความผิดพลาดได้ง่าย
* การทดสอบและ deploy มีความเสี่ยง
  * เมื่อมีการเปลี่ยนแปลงเพียงเล็กน้อยก็ต้องทดสอบและ deploy ทั้งระบบ และหากผิดพลาดจะกระทบผู้ใช้ทั้งหมด  
* ระบบล่มทั้งชุดได้ง่าย
  * หากส่วนใดส่วนหนึ่งมีปัญหา อาจทำให้ทั้งระบบไม่สามารถใช้งานได้ 

### วิธีแก้ไขด้วย Layered Architecture:
* ลดความซับซ้อนของโค้ด
  * แต่ละ layer มีหน้าที่ชัดเจน แก้ไขง่าย ไม่กระทบทั้งระบบ
* ดูแลและขยายระบบง่ายขึ้น
  * เปลี่ยน UI หรือฐานข้อมูลได้ โดยไม่กระทบ business logic
* รองรับการทำงานเป็นทีม
  * Frontend, Backend, Database ทำงานแยกกันได้
* ทดสอบง่าย
  * ทดสอบแต่ละ layer แยกกันได้ (Unit Test / Integration Test)
* ลดความเสี่ยงตอน deploy
  * การเปลี่ยนแปลงอยู่ในขอบเขตที่ควบคุมได้ 

### ประโยชน์ที่ได้รับ:
* โค้ดมีโครงสร้างชัดเจนและเข้าใจง่าย
* ดูแลและแก้ไขระบบได้ง่ายขึ้น
* สนับสนุนการทำงานเป็นทีม
* เพิ่มคุณภาพและความเสถียรของระบบ
* ทดสอบระบบได้ง่ายและมีประสิทธิภาพ

## 🚀 How to Run

```bash
# 1. Clone repository
git clone [your-repo-url]

# 2. Install dependencies
npm install

# 3. Run server
npm start

# 4. Test API
# Open browser: http://localhost:3000
```

## 📝 API Endpoints
| HTTP Method | Endpoint | Description |
|------------|----------|-------------|
| GET | `/api/students` | ดึงข้อมูลนักศึกษาทั้งหมด (รองรับ query parameters เช่น `status`, `major`) |
| GET | `/api/students/:id` | ดึงข้อมูลนักศึกษาตามรหัสประจำตัว (ID) |
| POST | `/api/students` | เพิ่มข้อมูลนักศึกษาใหม่ |
| PUT | `/api/students/:id` | แก้ไขข้อมูลนักศึกษาทั้งหมดตาม ID |
| PATCH | `/api/students/:id/gpa` | แก้ไขค่า GPA ของนักศึกษาตาม ID |
| PATCH | `/api/students/:id/status` | แก้ไขสถานะนักศึกษาตาม ID |
| DELETE | `/api/students/:id` | ลบข้อมูลนักศึกษาตาม ID |
